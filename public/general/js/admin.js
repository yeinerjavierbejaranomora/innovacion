// A $( document ).ready() block.
$( document ).ready(function() {
  
    /** buscamos el panel para poner las facultades disponibles */
    facultades=$('#facultades').clone();

    /** eliminamos el tap que previamente duplicamos */
    $('#facultades').empty();

    /** realizamos un ciclo */






});